apiVersion: broker.amq.io/v2alpha2
kind: ActiveMQArtemis
metadata:
  name: ex-aao
spec:
  deploymentPlan:
    size: 1
    image: quay.io/hgao/amq-openshift-image:amq76
  acceptors:
  - name: amqp
    protocols: amqp
    port: 5672
    sslEnabled: true
    expose: true
